# Overview

This lab focuses on systemd as the primary service manager in RHEL.

A custom service is created to simulate a long-running daemon process. The lab then transitions into scheduling tasks using systemd timer instead of cron.

Security controls are applied to harden the service and reduce the system's attack surface.
