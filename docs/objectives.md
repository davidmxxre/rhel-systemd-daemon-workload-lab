# Objectives

- Create a custom systemd service
- Use environment files for configuration
- Configure restart policies
- Implement service dependencies
- Replace cron jobs with systemd timers
- Disable and mask unnecessary services
- Apply systemd security hardening directives
- Restrict and audit scheduled tasks
